import 'package:flutter/material.dart';

class VelocidadePage extends StatelessWidget {
  const VelocidadePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
